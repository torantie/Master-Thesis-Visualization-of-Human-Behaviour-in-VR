#if (UNITY_EDITOR)

//using UnityEditor.Scripting.Python;
using UnityEngine;

public class LaunchSentimentAnalysis : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //PythonRunner.RunFile($"{Application.dataPath}/.Python/SentimentAnalysis/main.py");
    }

    // Update is called once per frame
    void Update()
    {

    }
}

#endif